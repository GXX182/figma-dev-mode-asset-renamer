{
  "name": "figma-dev-mode-asset-renamer",
  "version": "1.8.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "server": "node server/start-server.mjs",
    "server:foreground": "node server/server.mjs",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/test.mjs && node tests/server.test.mjs",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.134.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
